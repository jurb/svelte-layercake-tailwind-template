<style global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  body {
    font-family: "Poppins";
  }

  h1.title {
    @apply text-5xl;
    font-weight: 700;
  }

  h2.subtitle {
    @apply text-3xl;
    font-weight: 300;
  }

  h1 {
    @apply text-2xl;
    font-weight: 700;
  }

  h2 {
    @apply text-xl;
    font-weight: 700;
  }
  h3 {
    @apply text-lg;
  }

  a {
    @apply text-blue-600 underline;
  }
</style>
